# TODO: Fix Deprecation Warnings and Errors

## Tasks
- [x] Update `utils/storage/companyStorage.ts` to import `getInfoAsync` from `expo-file-system/legacy`
- [x] Update `app/CompanySetup/company-setup.tsx` to use `'images'` instead of `ImagePicker.MediaTypeOptions.Images`
- [x] Test the app to ensure no errors
